const hook = {
  conflict_event_fields:
    'The "event" and "events" fields conflict. You should only use one of them.',
  missing_events: 'You need to provide at least one event.',
};

export default hook;
