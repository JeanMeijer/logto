const hook = {
  conflict_event_fields:
    'El campo "event" y "events" entran en conflicto. Deberías usar solo uno de ellos.',
  missing_events: 'Necesita proporcionar al menos un evento.',
};

export default hook;
