const hook = {
  conflict_event_fields:
    'I campi "event" ed "events" sono in conflitto. Si dovrebbe utilizzarne solo uno.',
  missing_events: 'È necessario fornire almeno un evento.',
};

export default hook;
